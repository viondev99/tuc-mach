import React, { FC } from "react";
import classes from "./drop-down-list.module.scss";

const DropdownList: FC = () => {
  return <div className={classes.Container}>dropdown</div>;
};

export default DropdownList;
